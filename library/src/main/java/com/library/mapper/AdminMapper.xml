<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.library.mapper.AdminMapper">

    <!-- 登录查询：仅数据库真实字段 -->
    <select id="login" resultType="com.library.entity.Admin">
        select id,username,password
        from admin
        where username = #{username} and password = #{password}
    </select>

    <!-- 根据id查询管理员 -->
    <select id="selectById" resultType="com.library.entity.Admin">
        select id,username,password
        from admin
        where id = #{id}
    </select>

    <!-- 修改密码 -->
    <update id="updatePwd">
        update admin
        set password = #{newPwd}
        where id = #{id}
    </update>
</mapper>